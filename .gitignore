# fixtures and captured data — downloaded, never committed (see fixtures/README)
fixtures/**
!fixtures/*.py
!fixtures/*.sh
!fixtures/*.json
!fixtures/*.pdf
!fixtures/README.md
*.jpg
*.jpeg
*.png
*.heic
# ...except deck assets for a dated submission. Scoped deliberately narrow so this cannot
# whitelist capture photographs, which must never be committed (see fixtures/README.md).
!submissions/assets/**/*.jpg
!submissions/assets/**/*.png
*.ply
*.glb
*.obj
*.stl
!fixtures/reference_cad/
!fixtures/reference_cad/*.stl

# model weights
*.pt
*.pth
*.safetensors
weights/
checkpoints/

# secrets
.env
.env.*
!.env.example
*.pem
*.key

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
build/

# node
node_modules/
dist/
.vite/

# os / editor
.DS_Store
.idea/
*.swp

# personal / local-only — never committed
project-playbook.md
**/project-playbook.md
