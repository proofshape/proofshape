# F-05 · Shared GPU workspace provisioned, three-way access

**Area:** foundations · **Tier:** 1 · **Estimate:** 4 h
**Depends on:** nothing
**State:** Done
**Owner:** @mbj1994

---

## Acceptance criteria

- **Superseded by D-036:** shared Lightning GPU Studio available, with the Teamspace Drive designated as the durable shared location for model weights, checkpoints, and golden-capture data that must survive Studio recreation.
- All three members can connect and run a smoke test that confirms the GPU is visible to PyTorch.
- One-command startup script committed, and re-provisioning rehearsed once from scratch.

## Notes and decisions

2026-09-15: RunPod team account created and invitations sent to both teammates. Creating the
network volume requires adding a payment method, so the paid infrastructure portion is paused
until the team agrees how to fund the shared RunPod account. Keep F-05 Claimed by @mbj1994;
do not mark it Done or unblock F-07 until the network volume, GPU Pod, three-way access,
PyTorch GPU smoke test, startup script, and one clean re-provisioning rehearsal are complete.

This pause is a billing/coordination blocker, not a change to the technical acceptance criteria.
Work can continue on independent stories while the team decides payment.

2026-09-16: Evaluated Lightning AI as a no-upfront-payment alternative for the shared GPU
environment. A ProofShape Studio was created and the repository cloned successfully. On a
1x Tesla T4 (15 GB VRAM), `nvidia-smi` reported driver 580.178.04 / CUDA 13.0 and the Python
smoke test reported PyTorch 2.8.0+cu128, `torch.cuda.is_available() == True`, GPU `Tesla T4`,
and GPU count 1. This verifies GPU access for @mbj1994 only. F-05 remains incomplete: both
other team members still need to verify access, the team must agree whether Lightning replaces
the RunPod-specific infrastructure requirement, and the startup/re-provisioning criteria still
need to be completed.

2026-09-16: Added `scripts/check_gpu.py` as the shared PyTorch/CUDA smoke test and
`scripts/start_gpu_workspace.sh` as the one-command workspace check. Unit tests cover CUDA
available, CUDA unavailable, and missing-PyTorch behaviour without requiring a GPU in CI.
The full dependency/bootstrap environment remains intentionally out of scope here because F-07
owns reproducible environment construction.

2026-09-16: `bash scripts/start_gpu_workspace.sh` passed in the Lightning Studio on the T4.
After correcting the new test import/formatting and local execute-bit noise, the latest visible
repository checks reported `ruff check .` passing, all 49 files already formatted, and
`check_story_states.py` reporting all story states consistent. Keep the story Claimed until the
remaining team/manual checks below are real and recorded.

2026-09-17: The earlier 2026-09-16 status saying only @mbj1994 had verified access is now
superseded. The owner confirmed that @TabeenRaoof and @dalwalyk both accessed the shared
Lightning Studio and each ran `bash scripts/start_gpu_workspace.sh` successfully, so all three
members have completed the GPU smoke test. The team also completed the fresh-Studio
re-provisioning rehearsal and agreed to the RunPod → Lightning substitution recorded in D-036.

The owner confirmed 4 h actual work for F-05. The completion record intentionally remains in
template form until merge, when the real PR number and completion date are known, per the same
close-out process used by the other foundation stories.

For persistence, shared large assets are not tied to one Studio checkout: model weights,
checkpoints, and the F-06 golden capture belong in the Teamspace Drive, which is shared across
Studios. Studio-local environment/files may rely on Lightning's native persistence across sleep,
but they are not the only copy of project data. This preserves the original network-volume
intent without requiring a RunPod volume. R-15 keeps the separate HTTPS/TLS deployment proof;
D-036 changes the provider, not that requirement.

Manual verification still required before PR/merge:

- [x] @mbj1994: shared Studio opens and PyTorch sees the Tesla T4.
- [x] @TabeenRaoof: opened the shared Studio and ran `bash scripts/start_gpu_workspace.sh` successfully.
- [x] @dalwalyk: opened the shared Studio and ran `bash scripts/start_gpu_workspace.sh` successfully.
- [x] Team agreed that Lightning AI replaces the original RunPod-specific pod/network-volume
      implementation while preserving the intent: shared persistent GPU workspace, three-way
      access, and reproducible startup.
- [x] Re-provisioning rehearsal completed from a fresh Studio: repository cloned, F-05 branch
      checked out, GPU attached, and `bash scripts/start_gpu_workspace.sh` passed.
- [x] Full repository checks passed before PR: `pytest`, `ruff check .`,
      `ruff format --check .`, and `python3 scripts/check_story_states.py`.

## Completion record

```
Completed: @mbj1994 · 2026-09-21 · PR #21 · 4 h actual (est 4)
Notes: Shared Lightning GPU workspace verified by all three members; fresh-Studio reprovisioning completed.
```
