# F-02 · Repository skeleton and module boundaries

**Area:** foundations · **Tier:** 1 · **Estimate:** 3 h
**Depends on:** F-01
**State:** Done
**Owner:** @dalwalyk

---

## Acceptance criteria

- Top-level directories exist: `/recon`, `/capture`, `/inspect`, `/contracts`, `/fixtures`, `/scripts`.
- Python project installs with one command.
- `README` says how to run each part.

## Notes and decisions

The six top-level directories exist, each with its own `README.md` (needed anyway since git
does not track empty directories, and it's a natural place for run instructions to live next to
the code they describe).

For the Python packaging, only `recon/` is registered as an installed package
(`[tool.setuptools.packages.find]` with `include = ["recon*"]`). `inspect/` deliberately is
not: its name collides with Python's own standard-library `inspect` module, and installing it
as a top-level package on `sys.path` would shadow that module for the whole environment —
verified by installing both ways in a scratch venv and confirming `import inspect` resolves to
our package, not the stdlib, when `inspect/` carries an `__init__.py` and is included. Recorded
as D-030. This only blocks I-01 on picking an import name later; it does not block anything in
this story, since `recon/` is what the chain (F-02 → F-07 → R-01 → …) actually needs first.

Verified `pip install -e .` from a clean venv: installs cleanly, `import recon` works, and
`import inspect` still resolves to the standard library.

Scope note: `docs/sprint-plan.md` also mentions adding `CODEOWNERS` around the same time as repo
setup, but it isn't in this story's acceptance criteria (or F-01's), so it's left for whoever
picks that up rather than folded in here.

**Review round (@TabeenRaoof):** two blocking findings, one addressed by rebasing and one by
renumbering.
- The PR had gone stale against `main` (which had since merged F-01 → `Done`, F-02/F-03 →
  `Ready` in #2) — resolved by merging `main` in; the only real conflict was the three adjacent
  rows in `stories/README.md`, kept F-01/F-03 from `main` and F-02's own `In review` from this
  branch.
- The decision this story recorded had picked `D-025`, which by the time of review was
  independently claimed on two other open branches. Checked every open branch's
  `docs/decisions.md` at time of fixing (per the check `D-028` documents) and renumbered to
  `D-030` — the highest claimed anywhere at that point was `D-029`. Also added the regression
  test the reviewer suggested (`tests/test_packaging.py`, plus `pytest` as an optional `dev`
  dependency) so a future change that re-adds `inspect/__init__.py` fails a test instead of
  silently reintroducing the stdlib shadow.

## Completion record

```
Completed: @dalwalyk · 2026-09-14 · PR #6 · 3 h actual (est 3)
Notes: reviewed and approved by @TabeenRaoof.
```
