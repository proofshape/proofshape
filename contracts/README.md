# contracts

The frozen interface between `capture/` and `recon/`: `openapi.yaml`, describing session
creation, frame upload, reconstruction result, and verdict result.

Empty until F-03 freezes v1. After that, changing this file needs all three team members'
approval, at a sprint boundary only (see `CONTRIBUTING.md`, added by F-03).
