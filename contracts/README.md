# contracts — the frozen interface

`openapi.yaml` describes every request and response between the capture app and the reconstruction
service. It is written before either side is built, so both can be developed in parallel.

Changing it requires all three members to agree, at a sprint boundary only.

**Not yet populated.** Created by story F-03; see `../stories/README.md`.
