# capture

The progressive web app (TypeScript) — camera capture, local frame gate, gyro overlay,
next-view scoring, the model viewer, and the report page.

Nothing runs yet; there is no `package.json` here. The npm project and app shell arrive with
the capture stories (C-01 onward). It talks to `recon/` over the interface in `../contracts/`.
